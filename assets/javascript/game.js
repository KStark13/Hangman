 // VARIABLES==========================================================

    var words = [
        "wand",
        "Harry",
        "hogwarts",
        "butterbeer",
        "muggle"
        ];
 

      // Pick a random word
      var word=words[Math.floor(Math.random()*words.length)];


      // FUNCTIONS
      // ==============================================================================
     